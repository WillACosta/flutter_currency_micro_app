name: core_network
description: Network module
version: 0.0.1
publish_to: none

environment:
  sdk: '>=2.19.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  core_dependencies:
    path: '../core_dependencies'

dev_dependencies:
  flutter_test:
    sdk: flutter

  chopper_generator: ^6.0.0
  build_runner: ^2.3.3

flutter:
